# 1.0 release

Work that should land before tagging `1.0.0`. Not a feature wishlist — see [TODO.md](./TODO.md) and [MAYBE.md](./MAYBE.md) for later slices.

## 1. Land the indent fix

Uncommitted work in `parse` / `path` stores the real line prefix so insert does not write 2-space children under 4-space lists.

- [x] Keep each node's raw leading whitespace on parse
- [x] Child insert: copy first existing child prefix, else parent prefix + one indent unit
- [x] Sibling insert: copy the focused line prefix
- [x] Commit this before any other 1.0 packaging (`b173289`)

## 2. Rewrite the README for users

Current README still says HJKL is future, points at `model/` / `view/`, and never documents the actual interaction.

- [x] What it does, and that `#miller-view` on a `-` list is the trigger
- [x] Keyboard map: hover/Tab to enter, arrows/`hjkl`, Space, Enter / Shift+Enter, `x` / Delete, Alt/Ctrl+Enter, Escape
- [x] Limits: `-` only (not `*` or numbered), rename in source, reading / live-preview notes
- [x] Install: copy `main.js` + `manifest.json` + `styles.css`, or wait for the community listing
- [x] Screenshot or short GIF in the README

## 3. Strip sample-plugin leftovers

- [x] `package.json` `name` is not `obsidian-sample-plugin`
- [x] `package.json` description matches the plugin
- [x] License in `package.json` matches [LICENSE](./LICENSE) (MIT, not `0-BSD`)

## 4. Cut a real GitHub release

`manifest.json` is already `1.0.0` and `versions.json` maps it to `0.15.0`, but there is no tag.

- [x] Push `main` (including the indent commit)
- [x] Tag **`1.0.0`** with no `v` prefix
- [x] Attach `main.js`, `manifest.json`, `styles.css`

## 5. Run tests in CI

`.github/workflows/lint.yml` only does `build` + `lint`.

- [x] Add `npm test -- --run` to the GitHub Action before claiming 1.0

## 6. One manual pass in a vault

`isDesktopOnly` is `false` and insert still uses full-file `setValue` (undo is worse than toggle/delete).

- [ ] Reading mode
- [ ] Live preview
- [ ] Undo after toggle, delete, and insert
- [ ] Two `#miller-view` lists in one note
- [ ] Narrow / mobile width
